#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main ()
{
  int volumeBalok, panjang, lebar, tinggi;
  scanf("%d %d %d", &panjang, &lebar, &tinggi);
  volume= panjang*lebar + tinggi;
  printf("%d", volume);
  return 0;
}
